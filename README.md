# Universal Money Address (UMA) Python SDK

A Python SDK for implementing the Universal Money Address (UMA) protocol in your payment flow. UMA is an open protocol that makes sending money as easy as sending an email, using human-readable addresses like $alice@bank.com.

## Installation

```bash
pip install uma-sdk
```

Check out the [full documentation](https://docs.uma.me) for more info.
